import React from 'react';

const Footer = () => {
  return <div className={footerStyle}>links</div>;
};

const footerStyle = {
  backgroundColor: 'red',
  fontSize: '2rem',
};

export default Footer;
